# 全量测试失败清单（待统一修复）

本清单基于以下一次全量执行结果整理，目的是先归档失败现象与初步原因，后续再集中修复：

- 执行命令：`./venv/bin/python manage.py test`
- 执行目录：`EPP-Backend-Dev`
- 结果汇总：`Ran 116 tests`，`FAILED (failures=19, errors=9)`
- 日志来源：`terminals/145189.txt`

---

## 1. ERROR 用例（8）

### 1.1 `business.tests`（3）

- `test_comment_like`  
  - 现象：`KeyError: 'id'`  
  - 初步原因：响应体结构与测试预期不一致（测试直接取 `response.json()["id"]`）。

- `test_create_content`  
  - 现象：`KeyError: 'data'`  
  - 初步原因：响应体结构与测试预期不一致（测试直接取 `response.json()["data"]`）。

- `test_get_note`  
  - 现象：`TypeError: PaperNote() got unexpected keyword arguments: 'content', 'note_type'`  
  - 初步原因：测试构造模型字段与当前 `PaperNote` 模型定义不匹配。

### 1.2 `research_agent.tests`（6）

- `test_create_task_with_max_reflect_rounds`  
  - 现象：`KeyError: 'reflect_rounds'`  
  - 初步原因：结果结构变更后，`result` 中不存在该字段（或字段路径变更）。

- `test_admin_behavior_logs_sorting_supports_step_user_task_and_time`  
  - 现象：`TypeError: AgentBehaviorAuditLog() got unexpected keyword arguments: 'task'`  
  - 初步原因：审计日志模型字段已迁移，仍使用旧字段 `task`。

- `test_admin_can_filter_chain_and_export_behavior_logs`  
  - 现象：同上（`unexpected keyword arguments: 'task'`）  
  - 初步原因：同上。

- `test_user_can_report_behavior_log`  
  - 现象：`FieldError: Cannot resolve keyword 'task' into field`  
  - 初步原因：查询仍使用旧字段 `task`，当前模型为 `deep_task/basic_run/workspace_run`。

- `test_events_and_stats_api`  
  - 现象：`TypeError: AgentBehaviorAuditLog() got unexpected keyword arguments: 'task'`  
  - 初步原因：与行为审计相关测试仍依赖旧字段。

---

## 2. FAIL 用例（17）

### 2.1 大量 401（鉴权/测试夹具问题）——11 项

以下用例均表现为 `AssertionError: 401 != 200`，说明接口鉴权前提与测试预期不一致：

- `business.tests.test_annotation.TestAnnotation.test_comment`
- `business.tests.test_annotation.TestAnnotation.test_create`
- `business.tests.test_annotation.TestAnnotation.test_get_annotation`
- `business.tests.test_annotation.TestAnnotation.test_owned`
- `business.tests.test_manager_statistics.TestManagerStatistics.test_get_popular_paper`
- `business.tests.test_manager_statistics.TestManagerStatistics.test_get_popular_search`
- `business.tests.test_manager_statistics.TestManagerStatistics.test_visit_record`
- `business.tests.test_note.TestNote.test_create_note`
- `business.tests.test_note.TestNote.test_modify_delete_node`
- `business.tests.test_translation.TestTranslation.test_glossary_recommend`
- `business.tests.test_translation.TestTranslation.test_glossary_view`

### 2.2 业务文案/状态语义不一致——2 项

- `test_report_suppressed_after_admin_action`  
  - 现象：期望包含“暂时不可访问”，实际为“内容违规，无法展示”。  
  - 初步原因：错误文案已变更，测试断言未同步。

- `test_user_abort_running_task`  
  - 现象：期望状态 `aborted`，实际为 `archived`。  
  - 初步原因：任务终态语义或状态流转规则已变更，测试仍按旧语义断言。

### 2.3 `research_agent.tests` 与当前链路不一致——6 项

- `test_message_task_flow_approve`  
  - 现象：`phases.count("plan_decide") == 0`。  
  - 初步原因：该测试执行路径未产出深度研究四阶段，断言与实际链路/任务类型不匹配。

- `test_workspace_create_file_writes_content`  
  - 现象：`intro.md` 未创建（`False is not true`）。  
  - 初步原因：工作区写文件步骤未触发或工具执行路径/前置条件变化。

- `test_workspace_intent_uses_workspace_pipeline`  
  - 现象：期望 `["plan","workspace_agent","write"]`，实际 `["plan","basic","write"]`。  
  - 初步原因：pipeline 标识命名或返回语义已变，测试未同步。

- `test_download_success`  
  - 现象：`self.assertTrue(res.ok)` 失败。  
  - 初步原因：本地文件下载执行结果失败（需排查 URL、权限、allowlist 或 mock 前置）。

---

## 3. 运行时噪声（非最终统计项，但会干扰稳定性）

日志中多次出现 SQLite 并发表锁，例如：

- `OperationalError: database table is locked: deep_research_step`
- `OperationalError: database table is locked: deep_research_task`

这类错误主要来自带线程的审计/合规模块路径，在 SQLite 下容易出现，可能导致测试波动和非确定性失败。

---

## 4. 后续修复建议（执行顺序）

1. **先修模型/字段断裂（高优先）**  
   统一处理 `AgentBehaviorAuditLog` 相关测试中的旧字段 `task` 与旧反向关系名。

2. **再修 401 测试夹具（高优先）**  
   统一梳理 `business.tests` 的鉴权 mock/登录态前置，避免同类 401 批量失败。

3. **同步链路断言（中优先）**  
   逐项对齐 `research_agent.tests.test_api` 中 pipeline/phase/步骤 detail 的新语义。

4. **最后处理环境与稳定性（中优先）**  
   为 SQLite 并发表锁路径加隔离或降并发策略，必要时将相关用例标记为慢测/集成测。

