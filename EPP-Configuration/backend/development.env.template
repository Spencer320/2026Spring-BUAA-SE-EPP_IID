# Django 基础（必填）
SECRET_KEY = django-insecure-change-me-to-a-random-string

# 远程 ChatChat / 模型服务（本地开发可指向 127.0.0.1）
REMOTE_CHAT_CHAT_PATH = http://127.0.0.1
CHAT_CHAT_MANAGER_PORT = 8001
MODEL_BASE_PORT = 7861
CHAT_GLM_PORT = 7861
GLM3_OPENAI_PORT = 7861

CHATCHAT_CHAT_MODEL = qwen3.5:latest
CHATCHAT_EMBEDDING_MODEL = quentinz/bge-large-zh-v1.5:latest

# 科研智能助手 LLM（OpenAI 兼容接口）
RA_LLM_BASE_URL = https://api.example.com/openai/v1
RA_LLM_API_KEY = your-llm-api-key-here
RA_LLM_MODEL = deepseek-v3.1-terminus

# DeepSeek（论文解读等模块）
DEEPSEEK_BASE_URL = https://api.example.com/openai/v1
DEEPSEEK_API_KEY = your-deepseek-api-key-here

WKHTMLTOPDF_PATH = /usr/bin/wkhtmltopdf

# 数据库（本地默认 SQLite；生产可切换 MySQL）
DB_ENGINE = django.db.backends.sqlite3
DB_NAME = development.sqlite3
# DB_USER = epp
# DB_PASSWORD = your-db-password
# DB_HOST = 127.0.0.1
# DB_PORT = 3306

# Django 管理后台初始账号（首次 migrate 后生效）
ADMIN_USERNAME = admin
ADMIN_PASSWORD = change-me

SIMPLIFY_TRANS_KEY = your-simplify-trans-key-here

# 内容审核（百度等，可选）
CENSOR_API_KEY = your-censor-api-key-here
CENSOR_SECRET_KEY = your-censor-secret-key-here

JWT_SECRET_KEY = change-me-to-a-random-jwt-secret

# 科研智能助手（M2 出站 HTTP 白名单，逗号分隔主机名；M1 可留空）
RA_ALLOWED_HOSTS =
RESEARCH_AGENT_MOCK_DELAY = 0.08

# MinIO 对象存储
MINIO_ENDPOINT = 127.0.0.1:9000
MINIO_ACCESS_KEY = minioadmin
MINIO_SECRET_KEY = minioadmin

# Tavily 联网搜索（科研助手 Web Search）
TAVILY_API_KEY = your-tavily-api-key-here

# 学术 API（可选；不配则路由仅使用 arXiv / Crossref 等无需 Key 的后端）
# RA_SEMANTIC_SCHOLAR_API_KEY =
# RA_IEEE_XPLORE_API_KEY =
# Crossref Polite Pool 建议填写联系邮箱
# RA_CROSSREF_POLITE_EMAIL = your-email@example.com
# RA_ACADEMIC_SEARCH_LIMIT = 8

# Web Operator（Playwright）；部署后需执行: playwright install chromium
# RA_WEB_OPERATOR_ENABLED = true
# RA_WEB_OPERATOR_MAX_STEPS = 12
# RA_WEB_OPERATOR_GOTO_TIMEOUT_MS = 45000
# RA_WEB_OPERATOR_STEP_TIMEOUT_MS = 15000
